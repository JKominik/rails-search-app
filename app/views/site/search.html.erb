<h1>JUKEBOX</h1>

<%= form_tag site_search_path do%>
	<%= search_field_tag :search_term%>
	<%= submit_tag "Search"%>
<% end %>

<%= params%>

<hr>

<% if !@results.nil? %>
	<% @results.each do |f| %>
		<p><%= link_to f.name, f %></p><hr>
	<%end%>
<% end %>